## Packages
framer-motion | Smooth page transitions and UI animations
recharts | Visualizing confidence and severity scores
clsx | Conditional class merging
tailwind-merge | Class merging utility

## Notes
- Images are handled as Base64 strings for the analysis endpoint
- Mobile-first design approach
- Camera access via standard HTML file input with capture="environment"
